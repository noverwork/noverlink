# Noverlink Relay Configuration
# REQUIRED: Copy this file to .env and set all values

# WebSocket port for CLI connections
# When using Caddy reverse proxy: use internal ports (8444)
# Direct deployment (no proxy): use 443
WS_PORT=8444

# HTTP port for public traffic
# When using Caddy reverse proxy: use internal ports (8080)
# Direct deployment (no proxy): use 80
HTTP_PORT=8080

# Base domain for tunnel URLs (NO PORT, NO PROTOCOL)
# Examples:
#   Development (with Caddy):    localhost
#   Development (direct):        localhost:8080
#   Production (with Caddy):     noverlink.com
#   Production (direct):         noverlink.com:8080
#
# Note: When using Caddy reverse proxy, omit the port number.
#       Caddy handles HTTPS termination and forwards to internal ports.
BASE_DOMAIN=localhost

# Log level: error, warn, info, debug, trace
# Production: info | Development: debug
RUST_LOG=info

# REQUIRED: Shared secret for ticket verification
# Must be at least 32 characters and match TICKET_SECRET in backend
# Generate with: openssl rand -hex 32
TICKET_SECRET=your-32-character-minimum-secret-key-here

# REQUIRED: Backend API URL for session management and request logging
BACKEND_URL=http://localhost:3000

# REQUIRED: Shared secret for relay-to-backend authentication
# Must be at least 32 characters and match RELAY_SECRET in backend
# Generate with: openssl rand -hex 32
RELAY_SECRET=your-32-character-minimum-relay-secret-here

# Optional: Unique identifier for this relay instance
# Defaults to "relay-1" if not set
RELAY_ID=relay-1
