# Noverlink CLI Build Configuration
# These are BUILD-TIME environment variables, NOT runtime config.
# Set these before running `cargo build`.

# Backend API URL
# Development: http://localhost:3000
# Production: https://api.noverlink.com
#
# Example build command:
#   NOVERLINK_API_URL=https://api.noverlink.com cargo build --release
NOVERLINK_API_URL=http://localhost:3000
