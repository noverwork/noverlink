# Noverlink CLI Build Configuration
# These are BUILD-TIME environment variables, NOT runtime config.
# Set these before running `cargo build`.

# Backend API URL (include /api prefix)
# Development: http://localhost:3000/api
# Production: https://api.noverlink.com/api
#
# Example build command:
#   NOVERLINK_API_URL=https://api.noverlink.com/api cargo build --release
NOVERLINK_API_URL=http://localhost:3000/api
