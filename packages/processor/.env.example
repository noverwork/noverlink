# Relay Server Configuration
# ALL environment variables are REQUIRED - no defaults provided

# Agent connection port (REQUIRED)
RELAY_AGENT_PORT=9080

# HTTP API port (REQUIRED)
RELAY_HTTP_PORT=9081

# Log level (optional, defaults to info via tracing_subscriber)
RUST_LOG=info